# Issue Seeds (for `good first issue` / `help wanted`)

Use these as starter issues in GitHub.

1. Add keyboard shortcut hint tooltip to search input.
2. Add explicit loading state for Local AI install button.
3. Improve empty-state copy for zero search results.
4. Add unit tests for collection classification by file extension.
5. Add benchmark note section to release checklist.
6. Add docs example for verifying SHA-256 checksums in PowerShell.
7. Improve accessibility labels for right-panel action buttons.
8. Add guard for invalid root path before running index build.
9. Add localized status message for monitor start/stop transitions.
10. Add integration smoke script for Windows installer validation.
